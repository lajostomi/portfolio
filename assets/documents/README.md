# Resume file

Place the actual resume PDF here as:

    lajos-tamas-jakab-resume.pdf

The "DOWNLOAD RESUME" button on about.html already points at this exact path with a
`download` attribute, so the browser will auto-download it as soon as the file exists —
no code changes needed once you drop the PDF in.
